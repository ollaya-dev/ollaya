{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window: the app's own commands and events.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
